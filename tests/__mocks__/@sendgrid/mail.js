module.exports = {
	// since we are not expecting return values for these functions
	// we do not need to do anything except export empty functions with those signatures
	setApiKey() {},
	send() {},
};
